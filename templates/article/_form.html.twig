{{ form_start(form) }}

    <div class="row">
        <div class="col-lg-10">
            {{ form_row(form.titre) }}
        </div>
        <div class="col-lg-2">
            {{ form_row(form.date_article) }}
        </div>
    </div>
    {{ form_row(form.description) }}

    <button class="btn btn-primary btn-primary-custom mb-3">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
